#!/usr/bin/env bash
TITLE=$1

case ${TITLE} in

	scratchpad)
    # entry="urxvt -title scratchpad"
    entry="urxvt -T ${TITLE}"
    ;;
	FM)
    entry="urxvt -T ${TITLE} -e $HOME/.local/bin/bettercd.sh"
    ;;
  Neomutt)
    entry="alacritty -T ${TITLE} -e neomutt"
    ;;
  Todo)
    entry="urxvt -T ${TITLE} -e $HOME/.local/bin/schedule.sh"
    ;;
esac

xdotool search --onlyvisible --name "${TITLE}" windowunmap \
    || xdotool search --name "${TITLE}" windowmap \
    || $entry &
